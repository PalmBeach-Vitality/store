# Figma Content Studio docs (delivery via store agent)

These files belong in **PalmBeach-Vitality/pep** under `marketing/`.
This cloud agent was started on **store** and cannot push to pep (403).

Copy into pep PR #3 branch or main:

```bash
cp -R marketing/* /path/to/pep/marketing/
```

Canonical guide: `marketing/n8n-figma-content-studio.md`
