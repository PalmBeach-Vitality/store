# Sheets reference (PB Vitality)

**Every printed product string in one place:** [`../compound-identification-prompts.txt`](../compound-identification-prompts.txt) — the exact name, dose bar, concentration, footer and vial body for all 23 vial compounds, plus name and badge for all 30 pens, quoted verbatim from the mirrors below with the full label clause ready to copy. Regenerate with `python3 scripts/build_compound_identification_txt.py` after any label change; it is a report, never a source of truth.

| File | Tab name |
|---|---|
| `3-image-scenes-150.csv` | `3-image-scenes-150` (IG/FB Buffer image scenes — `aspect_ratio` **3:4** feed posts, not 9:16; writeback = `last_used_date` only) |
| `latest-models.csv` | Still lock — Grok Imagine Image 2.0 everywhere except `18-motsc-film-stills` (Flux.2 Max trial) |
| `3-image-scenes-150.csv` | `3-image-scenes-150` (IG/FB image scenes — writeback = `last_used_date` only) |
| `3-figma-content-queue.csv` | `3-figma-content-queue` (Figma Content Studio queue) |
| `4-reel-queue.csv` | `4-reel-queue` (finished Creatomate packages — WF B `sheets_append_reel`) |
| `5-reel-scenes.csv` | `5-reel-scenes` (630 Creatomate/Grok visual scene briefs) |
| `6-quality-variables.csv` | `6-quality-variables` (Grok Imagine quality tokens) |
| `7-unique-reel-creations-500.csv` | `7-unique-reel-creations-500` (legacy abstract scenes — **do not use for Imagine**) |
| `health_wellness_scene_settings_100.csv` | Source list of **100** lifestyle scene settings (input only) |
| `8-lab-items-500.csv` | `8-lab-items-500` (legacy subject list — not auto-synced from wellness rebuild) |
| `9-lab-item-creations-500.csv` | `9-lab-item-creations-500` (**production** Grok still/video — 500 rows; includes `still_edit_prompt`, models, motion; no Creatomate `mod_*`) |
| `13-chem-breakdown-54.csv` | `13-chem-breakdown-54` (**new** chemical-breakdown molecule vids — same columns as Sheet 9; 27 compounds × 2 ranks = 54 rows; `shot_family` / `camera_move` / `surface` / `lighting` / `color_grade` each have **6** staggered values; do not mix with vial Sheet 9) |
| `15-caption-science-27.csv` | `15-caption-science-27` (IG caption science briefs — **33** compounds; tab name kept; input for `peptide_caption_gen`) |
| `16-ig-captions.csv` | `16-ig-captions` (**new** caption archive — header + appended vial/pen captions after verify) |
| `14-pen-creations-150.csv` | `14-pen-creations-150` (**pens-only** catalog vids — Sheet 9 columns; **168 rows / 30 compounds**; **exactly 1 pen / 1 compound**; white clip-cap ON, white ridged dial, **crimson red on every row**, `3ml Pen` badge, no dose on the label; do not mix with Sheet 9 mixed lab rows or Sheet 13 molecules) |
| `17-seedance-25-t2v.csv` | `17-seedance-25-t2v` (**palmbeach-rx.com** Seedance 2.5 hyperrealistic T2V — 27 compounds; prompt / model / duration / resolution / aspect / audio / bitrate / wait on the sheet; native max 30s. Separate vitality.store T2V sheet is deferred.) |
| `21-sonilo-audio.csv` | Sonilo music+SFX columns for the joined MOTS-C reel (`sound_type=music_and_sfx`). Live write is `overlay_film_sonilo` onto Sheet 18. |
| `20-film-001-004-020-beach-entry.csv` | Snapshot of Sheet 18 overlay for FILM-001 / 004 (FILM-014 beach) + FILM-020 (space → atmospheric burn-up). Live write is `overlay_film_beach_entry`. |
| `19-film-join-25.csv` | Join-queue columns for the 25 MOTS-C film clips (also overlaid onto `18-motsc-film-stills`). VACE stitch + optional FLF2V seams — see `n8n-vace-clip-join.md` |
| `18-motsc-film-stills.csv` | `18-motsc-film-stills` (MOTS-C film stills + I2V stack — `video_provider` / `model_video` per beat: Seedance 2.5, Kling 3.0, Veo 3.1; Runway Gen-4.5 optional; finished cut **60–90s**) |
| `12-import-still-queue.csv` | `12-import-still-queue` (import path — same creative columns as Sheet 9 + `still_url` + `import_id`) |
| `10-creatomate-text-1000.csv` | `10-creatomate-text-1000` (Creatomate overlays: `product_name` + `mod_intro`/`mod_fact_*`) |
| `11-creatomate-render-queue.csv` | optional queue (legacy); WF B prefers Set node `video_url_input` — see `n8n-creatomate-package-workflow.md` |
| `10-creatomate-text-500.csv` | first 500 rows only (legacy; use 1000) |
| `8-lab-items-250.csv` / `9-lab-item-creations-250.csv` | Legacy compat copies (not updated by wellness rebuild) |
| `500_Peptide_Wellness_Reel_Scenes.csv` | Live landscape / lab-scene library (`500_Peptide_Wellness_Reel_Scenes`) — **601 vial rows, 23 compounds**, no pens; label strings mirror Sheet 9 |

## Image scenes (`3-image-scenes-150`)

Columns: `scene_id`, `scene_category`, `scene_name`, `lab_environment`, `camera`, `lighting`, `product_hero`, `product_form_detail`, `compound_id`, `compound_name`, `canonical_url`, `scene_brief`, `caption_lock`, `status`, `rotation_order`, `last_used_date`, `aspect_ratio` (**3:4** — Buffer Instagram feed posts reject 9:16).
Columns: `scene_id`, `scene_category`, `scene_name`, `lab_environment`, `camera`, `lighting`, `product_hero`, `product_form_detail`, `compound_id`, `compound_name`, `canonical_url`, `scene_brief`, `caption_lock`, `status`, `rotation_order`, `last_used_date`. Pen overlay may add `still_prompt`, `model_still`, `aspect_ratio` (`9:16`), `still_resolution`, `still_n` on **`pen_3ml_scene` only**. Do **not** rewrite `vial_10ml_scene` or `lab_scene` prompts.

Writeback after Buffer: **`last_used_date` only** (match on `scene_id`). Captions come from Grok → `Parse_Grok` → `Save_render_URL`, not this sheet.

## Reel Studio / Creatomate

- Grok still/video library: tab **`9-lab-item-creations-500`** (sheets-only inputs — see `n8n-sheets-only-vid-gen.md`)
- Still edit text is **not** a sheet writeback. Type it as Fixed on n8n `still_edit_instructions`. Column **`still_edit_prompt`** on Sheet 9/14 may still exist; vid-gen does not read or write it.
- Grok still/video library: tab **`9-lab-item-creations-500`** (sheets-only inputs — see `n8n-sheets-only-vid-gen.md`). Daily wire + edit-before-video: `n8n-vid-gen-lab-scenes.md`.
- Optional still edit text: column **`still_edit_prompt`** (blank = skip edit)
- **`compound_name` is a selector, not label text.** `grok_imagine_reel_still` sends only `video_prompt`, so the name printed on the vial is whatever that prompt says; `compound_name` just picks the row. `pull_sheet_row` matches it with a two-way substring test on the normalized string, so a name that sits inside another active name is unreachable — `Ipamorelin` can never be addressed while `CJC/Ipamorelin` is Active. Where that happens, put a collision-free handle in `compound_name` and keep the real name in `video_prompt`. Check with `scripts/simulate_choose_compound.py` before adding a compound.
- **A new row's `video_prompt` must name its compound and carry the hard locks.** It is the only field the still node sends, so anything it omits never reaches the render — a prompt that says "compound name in large bold dark maroon" and nothing else returns an unlabeled vial. All 535 rows now carry `LABEL REQUIREMENT`, `VIAL STATE RULE`, `SINGLE HERO PRODUCT RULE`, `PRODUCT COUNT MUST EQUAL 1` and `NO DOUBLES ANYWHERE`; verify with `scripts/verify_rows_against_workflow.py` before adding rows.
- **Write to this tab with `cellFormat: RAW`.** Under the Sheets default (`USER_ENTERED`) the string `9:16` is coerced into a time value, which is why `prep_grok_video_start` carries a decimal-to-`h:mm` fallback for `aspect_ratio`.

Sheet 9 history: the tab went 500 -> 535 rows on 2026-09-14 (7 vial compounds x 5 rows), and the 70 lockless rows were repaired in the same pass. Both were applied by a throwaway workflow that was archived afterwards; no existing workflow was modified. `9-lab-item-creations-501-535-new.csv` and `9-lab-item-creations-unlabeled-70-fix.csv` are the audit record of what changed. The builders that produced them refuse to run again while the mirror holds 535 rows.

A second pass the same day fixed four prompt defects, audit record `9-lab-item-creations-cleanup.csv`, script `scripts/cleanup_sheet9_prompts.py`: six rows described the hero as another compound's research display; that same phrase called the hero yellow-green while the row's own `VIAL STATE RULE` locked a different fill; twelve non-GLOW rows carried GLOW's blue liquid and three GLOW rows were colourless; and `scene_brief` held a stale copy of the prompt head on every row, 252 of them naming a foreign compound. `scene_brief` is now re-derived from each row's own `FULL SCENE BRIEF` section. The blue flip-off cap is a separate lock from the blue fill and was not touched.

A third pass the same day made the label render at all, audit record `9-lab-item-creations-label-fix.csv`, script `scripts/fix_vial_label_legibility.py`. A Cagrilintide still came back with an unreadable label, and the prompt was the reason three times over. Nothing in any of the 535 rows said how large the vial should be — no `fills`, no `frame width`, no scale instruction of any kind — while every row carried a detailed environment brief, so the model built a wide room shot that left the label about 155px across; type below roughly 20px of glyph height does not resolve into letterforms. Every row now carries a `HERO SCALE (MANDATORY)` clause putting the glass at 40-45% of frame width. The label spec also ordered a dose bar and a concentration line without ever saying what they read (440 rows stated no dose at all), and ask for text while withholding the words and you get text-shaped noise; both now carry exact strings. And `LABEL REQUIREMENT` banned research-use disclaimer lines while ordering label elements it never filled in, which is precisely the contradiction the model resolved by rendering generic fine print — it now enumerates the three or four strings the label may contain and calls anything else wrong. The footer was hardcoded `10ml` on all 535 rows, so the 5ml Cagrilintide vial printed the wrong volume; 29 rows are now `5ml`. Four Cagrilintide rows also had their scene brief cut mid-value, leaving `concentration line exactly '2.` with an unbalanced quote butted against `LABEL REQUIREMENT`. Doses are never invented: a row stating its own mg keeps it, so BPC-157 holds both its 10mg and 20mg SKUs, and everything else comes from `compound-vial-labels.json`.

A fourth pass specified the DNA emblem, script `scripts/fix_vial_helix_emblem.py`, payload `9-lab-item-creations-helix-fix.json`. The retest got text, size and position right and the helix wrong, for exactly the reason the dose bar had been wrong: all 535 rows spent nine words on it — `dark red double-helix DNA icon at top` — and the sheet contained no occurrence of `continuous`, `strand`, `rung`, `turns` or `emblem` anywhere. So the model invented the geometry and produced strands made of separate beads, two mirrored almond halves butted together rather than one twist, rungs hanging off a single strand like fringe at uneven lengths, and a smear where the halves met. A `HELIX EMBLEM (MANDATORY)` block now fixes continuous solid strands of even stroke weight, one horizontal axis, exactly four crossings, perpendicular rungs of equal length at even spacing, one unbroken piece of artwork at about half the label width — and forbids each defect from that render by name. The 70 rows that describe the icon a second time called it `dark red` once and `dark maroon` once; an element with two colour words drifts between runs, so both now read `dark red`. The maroon dose bar, which was running off the label's right edge, is inset with even margin. That emblem spec was guessed rather than measured and the fifth pass replaced it wholesale; only the dose bar inset survives.

A fifth pass matched the vial to the catalog artwork, script `scripts/match_catalog_vial_artwork.py`, payload `9-lab-item-creations-artwork-fix.json`. Salvatore supplied the BPC-157 product photo as the reference. Two things were wrong. The glass had no geometry at all: every row said `standard ~10ml multi-use vial proportions` — including the 29 rows printing a 5ml footer — with no cap, collar, neck, shoulder or proportion language anywhere, so one default slim body rendered at every volume and the 10ml read no wider than the 5ml. A `VIAL SHAPE (MANDATORY)` block now carries the measured stack (blue cap 95% of body width, silver collar 88%, glass neck 66%, shoulder flaring to full width; full height 2.3x the body width), plus a per-volume clause: the 10ml body is **1.2:1** tall-to-wide, measured off the photo, and the 5ml is **1.6:1**, derived from it rather than measured — there is no 5ml reference photo, so if Cagrilintide comes back too slim that ratio is the number to move. And the fourth pass had specified the emblem from imagination: thin even strands, one horizontal axis, four crossings, rungs of equal length at even spacing, half the label width, with beads and staggered rungs forbidden. The real mark is almost the inverse — two broad gradient ribbons on a **vertical** axis, about three crossings, stepped groups of short staggered rungs of unequal length, 7:4 tall and a **fifth** of the label width — so the old block was steering the model away from the brand mark and is gone. Label ink sampled off the artwork came back one colour in three places (bar `#A63334`, name `#9A2E2D`, ribbon `#A43F3A`), while the sheet named it `dark maroon`, `maroon` and `dark red` in eight different clauses; all of them now read `brick red #A63334`, the way Sheet 14 pins `crimson red #DC143C`. Printed strings were retyped to the real label: uppercase `25MG`, `5mg/mL` with no space, `Multi-Dose` not `Multi-Use`, `mL` not `ml`. This pass pushed prompts from ~6.9k to 7.4-9.4k characters; `pull_sheet_row` has no cap and passes `video_prompt` through verbatim, but if a still comes back missing the tail, length is the first thing to check.

A sixth pass, 2026-09-17, locked **10ml rows only** to Salvatore's BPC-157 10mg catalog photo, script `scripts/apply_measured_10ml_vial_spec.py`, payload `9-lab-item-creations-10ml-vial-spec.json`, spec `lab-vial-10ml-visual-spec.md`. The stills were coming back too tall, too narrow, and with an oversized helix. Two reasons: the still-side lock (`lab_item`, `material_detail`, `hero_style`, `still_edit_prompt`) still said only `standard ~10ml multi-use vial proportions` with a sizeless `DNA icon at top`, so Grok was free to draw a test-tube and a large logo; and the fifth-pass `VIAL SHAPE` numbers were off the same photo (full height 2.3×, body 1.2×, collar 88%). Re-measured from his photo: full height **2.36×** body width, straight body **1.56×**, cap 94%, collar 92%, neck 66%, top cluster 34% / body 66%, helix one fifth of the label and 1.7× as tall as it is wide. FORBIDDEN: tall / slim / test-tube / ampoule, height over 2.4× body width, oversized helix. **506 10ml rows** on Sheet 9. The same numbers went onto `500_Peptide_Wellness_Reel_Scenes` (**577 10ml / 24 5ml**), payload `500_Peptide_Wellness_10ml-vial-spec.json` — shape on `material_detail` / `hero_style` / `video_prompt`, small helix on `video_prompt` only. The 29 lab and 24 wellness Cagrilintide rows that still say `This is the 5ml multi-dose vial` are untouched.

An eighth pass folded held PR #76 into this branch: **Melanotan II → Melanotan 2** on Sheet 9 (`PBVita-Lab-511`–`515`) and 24 wellness rows — selector and print name. Script `scripts/rename_melanotan2.py`, payloads `9-lab-item-creations-melanotan2.json` (5), `500_Peptide_Wellness_melanotan2.json` (24). The 10ml lock and Wolverine print-name work stay. Pens already printed Melanotan 2. The vial/pen Melanotan spelling clash is gone; Semax/SEMAX remains. Live sheets: unpublished `apply_melanotan2_rename` (`n85xPWY4xY3LdfhY`) exec **2254** (5 lab + 24 wellness); read-back `verify_melanotan2_rename` (`Rz6i6fav0a06V2XR`) exec **2255**.

A seventh pass the same day, after lab smoke `Vid_gen_lab_scenes` exec 2251, printed **BPC-157/TB-500** instead of **Wolverine** on Grok-facing fields. xAI `grok-imagine-image-2.0` word-blocks Marvel IP; PBVita-Lab-207's `video_prompt` said `reading exactly 'Wolverine'`. Script `scripts/rename_wolverine_print_name.py`, payloads `9-lab-item-creations-wolverine-print.json` (29), `500_Peptide_Wellness_wolverine-print.json` (32), `14-pen-creations-wolverine-print.json` (5). `compound_name` / `choose_compound` stay **Wolverine** so typing Wolverine still picks the row. Product URLs (`wolverine-vial`, `wolverine-pen`) are not rewritten. `alias_stack_names` still maps the handle; it no longer rewrites prompt text `BPC-157/TB-500` back to Wolverine. Live sheets: unpublished `apply_wolverine_print_name` (`74OyZ0RWA6d9KdIr`) exec **2252**; read-back `verify_wolverine_print_name` (`LnKj6WPzoVFrknxg`) exec **2253** (29 / 32 / 5, Lab-207 handle still Wolverine).

`compound-vial-labels.json` was wrong on two entries, corrected in the same pass. The Tesamorelin/Ipamorelin **vial** is 10ml (3ml is the pen), and its `conc` is now `null` on purpose — the label prints `12mg/3mg` and no mg/ml line, so nothing should derive one. KPV sat under `no_vial_sku` even though the price sheet carries `KPV 10mg Vial`; it now has an entry. Two disagreements are left open rather than guessed: the catalog says `CJC` is 100mg / 10 mg/ml while the five live CJC rows print 10mg / 1 mg/ml from the price sheet's `CJC-1295 10mg Vial`, and those are arguably two different SKUs (DAC vs no-DAC).

The `-250` files (`9-lab-item-creations-250.csv`, `../pbvita-250-lab-item-creations.json`) are **not** a second mirror of this tab. They are an older generation of the same `creation_id`s and differ on ~440 rows. `scripts/mirror_live_sheet9.py` used to overwrite them and no longer does. Decide whether to keep or delete them; until then, leave them alone.
- **Vial state (CRITICAL):** upright only; exactly one vial; pre-filled before still (never filling in video); clear liquid except **GLOW** = bright blue. Script: `scripts/enforce_vial_state_rules.py`
- **Single hero product (CRITICAL):** exactly **one vial OR one pen** per creation image — never both, never multiples. Script: `scripts/enforce_single_vial_or_pen.py`
- Vial look (Sheet 9 / 8 / 12): clear glass + **blue flip-cap** + silver crimp + white label with maroon DNA logo / compound name / maroon dosage bar / `<vol> Sterile Multi-Use Vial` footer using the compound's own volume, never a flat 10ml — see `scripts/enforce_pbvita_vial_packaging.py`. **Sheet 9 and `500_Peptide_Wellness_Reel_Scenes` have both moved past this wording** to the catalog-artwork spec: `brick red #A63334`, `<vol>mL Sterile Multi-Dose Vial`, uppercase `MG`, `mg/mL` — see `scripts/match_catalog_vial_artwork.py`.
- **Vial dosages (CRITICAL):** maroon bar + black mg/ml must match `marketing/compound-vial-labels.json` (price list + Salvatore confirms). Sermorelin is **20mg / 2 mg/ml**, never 5mg. Script: `scripts/lock_vial_dosages.py`
- Vial look (Sheet 9 / 8 / 12): clear glass + **blue flip-cap** + silver crimp + white label with maroon DNA logo / compound name / maroon dosage bar with **catalog mg + mg/ml per compound** / volume footer — see `scripts/overlay_lab_vial_dosages.py` and `compound-vial-labels.json`
- Import stills: tab **`12-import-still-queue`** (do not paste URLs into Fixed n8n fields)
- Chemical-breakdown molecule vids: tab **`13-chem-breakdown-54`** (Sheet 9 columns; dark microscopic **cellular chemical reaction** — living cells + amino acids; no logo, no text, no sound; not a vial, not a pen). `shot_family`, `camera_move`, `surface`, `lighting`, `color_grade` each have **6** staggered values so consecutive ranks never match.
- Pens-only catalog vids: tab **`14-pen-creations-150`**, workflow `peptide_pen_vid_gen` (`eLM4xCpHflgqJGfB`). Sheet 9 **columns**, pen **input** from `3-image-scenes-150`. **168 rows, 30 compounds.** Exactly one white matte longer-barrel insulin-style 3ml pen, white clip-cap ON, white ridged dial, crimson red DNA helix (no hands) + compound name + `3ml Pen` badge. No orange, no vial, no production row, no second pen, and **no milligram dose** — the prompt forbids it, so pen rows need no dose research even though pen doses differ from vial doses. Type **GLOW**, **KLOW**, or **Wolverine** on `choose_compound` the same as any other SKU.
- Landscape vial vids: live tab **`500_Peptide_Wellness_Reel_Scenes`**, workflow `Vid_gen_landscape_scenes -500-peptide-wellness-scenes` (`Kc2HqqjSyiKs87qy`), mirrored in this repo as `500_Peptide_Wellness_Reel_Scenes.csv`. Same type-in: **GLOW**, **KLOW**, **Wolverine** — those nicknames are the live `compound_name` values now, so no alias node is needed (this workflow has none). Re-sync with `scripts/mirror_live_wellness_sheet.py`, check with `scripts/audit_wellness_sheet.py`.
- **The landscape tab is vials only and carries the same vial spec as Sheet 9.** Rebuilt 750 → **601 rows / 23 compounds** on 2026-09-14, the same 23 Sheet 9 carries, every compound at 5 rows or more. The vial spec blocks are **imported** from `scripts/match_catalog_vial_artwork.py` by `scripts/rebuild_wellness_sheet.py` rather than copied, and label strings are lifted from Sheet 9's approved set, so the two tabs print identical labels for the same compound and cannot drift apart. What the audit found and the rebuild fixed:
  - **486 of 750 `video_prompt` values were 805 characters of boilerplate and nothing else** — byte-identical across all 486, no compound, no dose, no scene. The scene copy was never missing: it sat in the `surface` column behind a `SINGLE HERO SPOTLIGHT` prefix, and every scene sentence opened with its own bottle descriptor (`a glowing amber wellness vial`, `a slim silver-capped peptide bottle`) that contradicted the hardware lock. The lead descriptor is stripped so the lock governs appearance and the scene governs action.
  - **No row stated a hero scale**, so nothing made the label render large enough to read — the same defect that made the Sheet 9 stills illegible. All 601 now carry the 40-45% frame-width clause.
  - **545 rows printed a label that was not their own compound**, and 195 were told to `copy the GHK-Cu catalog still` whatever their own compound was.
  - **47 `pen_3ml` rows carried a `VIAL VISUAL LOCK`** on top of their pen spec, so the prompt ordered one vial, one pen, and then said `Never both`. Dropped — pens live on Sheet 14.
  - **Dropped 5-Amino-1MQ and DSIP** (`no_vial_sku` in `compound-vial-labels.json`, so a vial row rendered a product that does not exist) and **Semaglutide, Tirzepatide, Retatrutide**.
  - **`compound_name` aligned to the Sheet 9 roster**, which also cleared the selector collisions: typing `CJC` used to land on `CJC/Ipamorelin` and `Tesamorelin/Ipamorelin` used to land on `Tesamorelin`. One value was `'CJC '` with a trailing space. Handles now match Sheet 9 (`CJC-1295` prints `CJC`, `Tesa-Ipa` prints `Tesamorelin/Ipamorelin`, `Ipamorelin-Solo` prints `Ipamorelin`, `TA-1` replaces `Thymosin Alpha-1`).
  - **Cagrilintide** was spelled `Cagrilinitide` and printed a `10ml` footer against a 5ml vial. 24 rows now print `5mL` and get the narrow-body clause.
  - **Label ink was named four ways** (`dark maroon` 2987, `maroon` 955, `dark red` 750, `crimson` 498). Now one: `brick red #A63334`, as on Sheet 9.
- **Node conflicts on `Kc2HqqjSyiKs87qy` — repaired 2026-09-15** with Salvatore's OK:
  - `save_still_url` fed **`still_edit_instructions`** instead of `skip_still_edit`, putting the still-edit desk on the live path and spending a second paid API call every run. The wire now goes to `skip_still_edit`, matching lab and pen. The **wire** was the defect, not the text: `still_edit_instructions` is a fluid scratch node Salvatore types over per run, so whatever prompt is sitting in it is not a lock and is not an error. Do not audit it and do not report it (Salvatore, 2026-09-15).
  - `grok_video_poll` authenticated with an unlabeled duplicate header credential while the other two xAI calls used `XAI Grok`. Now all three share `XAI Grok`.
  - **`assert_video_ok` added** between `grok_video_poll` and `save_video_url`. The poll fires once after `wait_video`; if the render failed or was still queued, `save_video_url` wrote an empty `video_url` and `sheets_update_creation` still incremented `times_used`, so the row rotated out with nothing to show. Same guard pen has had all along. If it throws with `queued` or `processing`, raise `wait_seconds` on the row.
  - `prep_grok_video_start` read the sheet's `audio` column and then hard-coded `audio: false` in the request body anyway — a column `pull_sheet_row` refuses to run without, and ignored. Resolved in favour of the hardcode, not the column: **audio is off on all three vid-gen workflows, always** (Salvatore, 2026-09-15). The sheet read is gone, the body forces `audio: false`, and the motion prompt now carries the same silent lock lab and pen use. Leave `FALSE` in the `audio` column to satisfy the non-blank check; nothing reads it. See `.cursor/rules/no-hardcode-unless-asked.mdc`.
- **Still open on `Kc2HqqjSyiKs87qy`** — reported, not edited:
  - `save_video_url` names eight Set fields with a leading `=` (`=video_url`, `=creation_id`, …). Cosmetic only — n8n strips the `=` when resolving the field name, so these resolve correctly and `sheets_update_creation` is not relying on its fallbacks. Left alone.
  - The sheet's `still_edit_prompt` column is **read by nothing** — `prep_still_edit` takes the edit text only from the `still_edit_instructions` node, and `save_still_url` blanks the field. It used to hold the exact dose / concentration / footer strings on 208 rows, which is why the label strings were invisible to the image API.
  - `category` still reads `vial_10ml` on the 24 Cagrilintide rows, which are 5ml. Cosmetic only — no node composes `category` into a prompt.
- **Every pen on Sheet 14 is crimson red — the same exact red, worded identically.** Salvatore took Semaglutide, Tirzepatide and Retatrutide off the tab on 2026-09-14, so there is no cobalt-blue SKU left and no metabolic exception in the prompt. The house red is **`crimson red #DC143C`** — the phrase and the number together, on every red in `video_prompt` and `still_edit_prompt`, all 3,626 of them. `dark red`, `crimson/dark-red` and every other variant are gone, because a prompt that names two reds for the same part renders a red that drifts between shots, and a prompt where only half the reds carry a number reads the same way. `#DC143C` is the standard named colour `crimson`, chosen as the closest number to the phrase already approved; no other colour number appears on the tab. The one red word that stays unnumbered is `burgundy vial branding`, which lives inside a FORBIDDEN clause. Enforce with `scripts/unify_pen_red.py` then `scripts/pin_pen_red_and_spec.py`.
- **Sheet 14 carries no vial spec.** `lab_item`, `material_detail` and `hero_style` used to open with a 716-character `VIAL VISUAL LOCK` on all 168 rows, as did 19 `scene_brief` values — glass vial, blue flip-off cap, 10ml multi-use, maroon dose bar. Each now opens with the row's own `PEN VISUAL LOCK`. None of those columns reaches an API, but a pen tab should not describe a vial. The workflow's canvas sticky note still describes the old red/blue split and still names Semaglutide as a `choose_compound` example — stale, and not edited because node edits need Salvatore's OK.
- **Sheet 14 prompts were carrying three blocks twice, and the ceiling cut the second copy off.** Each `video_prompt` repeated the `HARD OUTPUT LOCK (READ FIRST)` block inside `FULL SCENE BRIEF`, restated the whole `FORM` section under `Supporting notes` as `product_form_detail`, and printed `LABEL (MANDATORY)` a second time at the end. That took the value to ~8.5k, where the trailing `LABEL` copy was cut mid-word on 139 rows and mid-sentence on 29 — every row ended in a fragment before its closing `LABEL: only ...` line. Dropping the second copy of each leaves 6.0–6.5k and loses nothing: all three removals were verbatim repeats of text that still appears earlier in the same prompt, so every hard lock and the printed label still read once. Script: `scripts/trim_pen_video_prompts.py`, payload `14-pen-creations-trim.json`. The builder refuses to run again once the duplicates are gone. One pre-existing wart survives on purpose — `Supporting notes` ends `A exactly one capped pens`, a template substitution artifact, not truncation.
- **Sheet 14 selectors follow Sheet 9.** `pull_sheet_row` matches `compound_name` with a two-way substring test, so `CJC-1295`, `Tesa-Ipa` and `Ipamorelin-Solo` hold the selector while the pen still prints `CJC`, `Tesamorelin/Ipamorelin` and `Ipamorelin`. Check with `scripts/audit_pen_sheet.py` before adding a compound.
- IG captions (vial + pen): tab **`15-caption-science-27`** in, **`16-ig-captions`** out — research language only, no “human use” / “benefits of using”
- Creatomate text: tab **`10-creatomate-text-1000`**
- Finished packages log: tab **`4-reel-queue`**
- Creatomate / Buffer packages: **no music** (muted only)
