# Deprecated — use Set-node Workflow B

**Canonical:** [`n8n-creatomate-package-workflow.md`](./n8n-creatomate-package-workflow.md)

Each run: paste the **NEW** Grok/vidgen URL into `video_url_input`.  
Copy `get_reel_text` → `save_creatomate_url` (+ Sheets updates). No catbox/queue required.
