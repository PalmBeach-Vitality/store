# Deprecated — use Set-node Workflow B

This Sheet-queue approach is superseded.

**Canonical Workflow B:** [`n8n-creatomate-package-workflow.md`](./n8n-creatomate-package-workflow.md)

Paste the `.mp4` URL into Set node `video_url_input` → copy `get_reel_text` … `save_creatomate_url` from the combined workflow.

Sheet `11-creatomate-render-queue` and `n8n-code-pick-creatomate-queue.js` / `n8n-code-map-creatomate-from-queue.js` remain only if you prefer a queue later.
